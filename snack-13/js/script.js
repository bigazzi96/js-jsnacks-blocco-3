const usernumber = 1234;

function inverti (number){
const string= number. toString();
let revesedString='';

for(let index = string.length - 1;index > 0; index --∏{
    revesedString+= string[string];
}

return revesedString;

const usernumber = number parseInt